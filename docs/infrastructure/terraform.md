# Terraform

## Structure

Document your Terraform module layout (per-environment folders, shared modules).

## State management

Where is state stored (S3 backend, locking via DynamoDB, etc)?

## Common commands

```bash
terraform plan
terraform apply
```
