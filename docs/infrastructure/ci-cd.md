# CI/CD

## Pipeline overview

What triggers builds, what runs on PR vs merge to main, how images get built
and pushed.

## Environments

Document promotion path: PR → staging → production, and what gates each step
(tests passing, manual approval, etc).
