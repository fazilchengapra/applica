# Coding Standards

## Python / Django

- Style (black, flake8/ruff, import ordering)
- App structure conventions within Django apps (services.py, selectors.py, etc if used)

## API conventions

- URL naming, versioning strategy
- Error response shape (consistent across all apps/services)
